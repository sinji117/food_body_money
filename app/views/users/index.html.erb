<%= render "shared/header" %>
<p id="notice"><%= notice %></p>

<h1>自分の日記</h1>

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>時間</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= current_user.nickname %></td>
        <td><%= current_user.start_time %></td>
        <td><%= link_to 'Show', user_path(current_user) %></td>
        <td><%= link_to 'Edit', edit_user_path(user.id) %></td>
        <td><%= link_to 'Destroy',user_path(user.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '日記をかく', new_user_path %>

<%= month_calendar events: @food do |date, food| %>
<%= date %> あなたの基礎代謝<%= current_user.metabolism %>kcal <br>

  <%# 現状八日ならみかんが出る%>
  本日取り込んだ物
  
<% sum = 0 %>
<% food.each do |f| %>
    <div>
      <%= f.product %>
      <%= f.calorie %>kcal
      <%# 現状商品名を押すと詳細ページに行く%>
      <% sum += f.calorie  %>
    </div>
  <% end %>
  現在<%= sum %>kcal取り込み中<br>
  <%= current_user.metabolism - sum %>kcal
  (マイナスはオーバーだよ！プラスならすごいね！)

<% end %>